# {项目名} — 小说创作工具集

## Skill 路由表

### 📚 学习支柱
| 命令 | Skill | 说明 |
|------|-------|------|
| `/novel-learn`、`/学习` | novel-learn | 学习入口：市场调研 / 小说分析 / 逆向导入 |

### ✍️ 写作支柱
| 命令 | Skill | 说明 |
|------|-------|------|
| `/novel-inspiration`、`/灵感` | novel-inspiration | 灵感迭代生成（9.0+ 分达标） |
| `/novel-outline`、`/大纲` | novel-outline | 大纲生成：全书大纲 → 章节大纲（两阶段） |
| `/novel-chapter`、`/正文` | novel-chapter | 章节写作：单章 / --batch 批量模式 |
| `/novel-quality`、`/评分` | novel-quality | 评分与优化（三量表 + 靶向优化） |
| `/novel-deslop`、`/去AI味` | novel-deslop | 去除 AI 写作痕迹（6 Gate） |

### 🔧 共享基础
| 命令 | Skill | 说明 |
|------|-------|------|
| `/novel-writing`、`/写作技法` | novel-writing | 核心技法库（角色/情节/正文） |
| `/novel-setup`、`/准备写小说` | novel-setup | **基础设施部署 + 项目自动管理**（世界观/一致性/伏笔/进度/备份） |
| `/novel`、`/小说` | novel | 工具箱路由 · 模糊意图自动分发 |

## 文件结构

```
{小说名}/
├── 灵感/           — 灵感卡片（novel-inspiration 输出）
├── 大纲/           — 全书大纲 + 章节大纲（novel-outline 输出）
├── 设定/           — 世界观、角色设定（自动管理 + 手动管理）
│   ├── 世界观.md
│   ├── 角色/
│   └── 势力/
├── 正文/           — 正文章节（novel-chapter 输出）
├── 追踪/           — 进度、伏笔、时间线（novel-supervisor 自动维护）
│   ├── 进度.md
│   ├── 伏笔.md
│   ├── 时间线.md
│   └── 写作反馈.md  — 管理→写作的核心桥梁
└── 素材/           — 市场调研等（novel-learn 输出）
```

## 协作规则

### 去耦合化创作架构（核心设计）

本项目以**去耦合化的角色特点×情节元素**为核心创作方法：

**角色特点分类库**（`novel-writing/references/character-traits.md`）：
- 4 维独立轴：个性类型(16种) × 情绪状态(16种) × 行为动作(7类40+) × 语言风格(18种)
- 公式：`角色表现 = 个性类型 × 情绪状态 × 行为动作 × 语言风格`

**情节元素分类库**（`novel-writing/references/plot-elements.md`）：
- 3 维独立轴：事件类型(20种) × 事件风格(14种) × 场景氛围(13种)
- 公式：`章节内容 = 事件类型 × 事件风格 × 场景氛围 × 角色特点`

**使用原则**：
- 同一角色在不同场景必须使用不同的情绪×行为×语言组合
- 不同角色不得重复同一组合
- 每章声明核心体验类型并极端化
- 优化时通过更换元素组合突破瓶颈，而非简单"加料"

**扩展库**：`novel-writing/extensions/` 存放 novel-learn 从分析小说中提取的新元素

### 三支柱协作
- **学习 → 写作**：novel-learn 的市场调研和技法扩展服务于写作流程
- **写作 → 管理**：每章完成后 novel-supervisor 自动更新世界观/进度/伏笔
- **管理 → 写作**：novel-supervisor 维护的写作反馈是所有创作 skill 的约束源

### 自动管理
- 系统通过 PostToolUse hook + novel-supervisor agent **自动审查**所有写作产出
- 每当 `正文/` 或 `设定/` 文件被修改，自动触发一致性检查 + 追踪更新 + 反馈生成
- **无需手动触发**，系统自动执行

### 反馈循环
```
正文生成 → 自动审查 → 写作反馈 → 下一章读取反馈 → 正文生成
    ↑                                              ↓
    └──────────────────────────────────────────────┘
```

### Agent 协作
- `novel-architect`：世界观架构 / 大纲设计 / 伏笔规划（使用 plot-elements.md 选取情节元素组合）
- `novel-writer`：正文写作 + 情绪执行（使用 character-traits.md + plot-elements.md 选取去耦合组合）
- `novel-character`：角色设计 / 对话风格 / 关系网络（使用 character-traits.md 选取四维角色组合）
- `novel-checker`：一致性检查（只读，含去耦合一致性维度）
- `novel-supervisor`：**自动管理**（审查 + 追踪 + 反馈，含去耦合化建议）
- `novel-learner`：市场调研 / 小说分析 / 技法提取（提取去耦合元素到 extensions/）

## 项目清单（manifest.md）

`追踪/manifest.md` 是项目状态的唯一真相源，所有 skill 在执行前后应读写此文件。

```markdown
# 项目清单：{小说名}

## 当前阶段
- 阶段：[灵感/大纲/章节大纲/写作/完成]
- 最后更新：[YYYY-MM-DD HH:MM]

## 已完成步骤
- [x] 灵感迭代 → 灵感/inspiration_*.md
- [x] 大纲生成 → 大纲/outline_*.md
- [ ] 章节大纲 → 大纲/chapter_outlines/
- [ ] 正文写作 → 正文/chapter_*.md

## 关键文件路径
- 灵感文件：灵感/inspiration_*.md
- 大纲文件：大纲/outline_*.md
- 世界观文件：设定/世界观.md
- 角色文件：设定/角色/*.md
- 进度文件：追踪/进度.md
- 伏笔文件：追踪/伏笔.md
- 写作反馈：追踪/写作反馈.md

## 写作进度
- 章节大纲：已生成 X/Y 章
- 正文：已生成 X/Y 章
- 目标字数：X 万字
- 当前字数：X 万字
```

## Compact 后恢复上下文

此部分在 compact 后自动生效。CLAUDE.md 在每次 compact 后会被重新加载。
写作中的关键上下文：
1. 当前写作项目名称和进度
2. 最近讨论的角色设定变更
3. 未完成的伏笔列表
4. 当前章节的情绪/节奏目标

如果存在 追踪/上下文.md，compact 后首先读取恢复上下文。
